# Vite env vars must be prefixed with VITE_
# Mastra backend hostname (no protocol, no path). Examples:
#   scarce-raspy-tiger.mastra.cloud
#   localhost:4000
VITE_MASTRA_API_HOST=scarce-raspy-tiger.mastra.cloud

# Optional: agent slug/id to talk to
VITE_WEATHER_AGENT_ID=weatherAgent
