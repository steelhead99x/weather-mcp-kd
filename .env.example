# Weather Agent KD - Example Environment Variables
# Copy this file to `.env` and fill in the values as needed.
#
# Notes:
# - The default dev entry (npm run dev / npm start) runs the advanced weather agent
#   which REQUIRES an Anthropic API key.
# - TTS is optional. If you set either CARTESIA_API_KEY or DEEPGRAM_API_KEY,
#   the agent will generate an MP3 file. If neither is set, it will save the
#   generated text to a .txt file instead.
# - WEATHER_MCP_USER_AGENT is recommended courtesy for Weather.gov requests.

############################################
# Core AI (Required for advanced agent)
############################################
# Get an API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Optional: override the Anthropic model used for generation
# Defaults in code: "claude-3-haiku-20240307"
#ANTHROPIC_MODEL=  # Example override (commented out)

############################################
# Text-to-Speech (Optional)
############################################
# 1) Cartesia TTS (preferred if set)
# Get an API key: https://cartesia.ai/
CARTESIA_API_KEY=
# Optional voice ID (default provided in code). Example shown here is a placeholder.
CARTESIA_VOICE=6f84f4b8-58a2-430c-8c79-688dad597532
# Optional model and sample rate (applies to mp3 and wav). Defaults to 44100 if unset.
#CARTESIA_TTS_MODEL=sonic-2
#CARTESIA_SAMPLE_RATE=44100
#
# 2) Deepgram TTS (fallback if Cartesia not configured)
# Get an API key: https://deepgram.com/
DEEPGRAM_API_KEY=
# Optional voice/model (default: "aura-asteria-en"). Code accepts DEEPGRAM_TTS_MODEL or DEEPGRAM_VOICE.
DEEPGRAM_VOICE=aura-asteria-en
#DEEPGRAM_TTS_MODEL=

############################################
# Weather.gov courtesy header (Optional, Recommended)
############################################
# Please set a descriptive user agent with contact info per Weather.gov guidelines.
# Example: "YourAppName/1.0 (youremail@example.com)"
WEATHER_MCP_USER_AGENT=WeatherAgentKD/0.1 (me@example.com)

############################################
# Misc (Optional)
############################################
# Mentioned in README for the minimal CLI variant; safe to leave unset here.
AGENT_NAME=weather-agent


############################################
# Test Scripts I/O (Optional)
############################################
# STT input audio file path (for src/scripts/test-stt.ts)
STT_INPUT_FILE=files/sample.wav
# STT output transcript file. If ends with .json, a JSON object with provider keys is saved.
# Otherwise a text file with labeled sections is saved.
STT_OUTPUT_FILE=files/sample-transcript.txt
# STT provider override: deepgram | cartesia | both
STT_PROVIDER=both

# TTS text input (already supported)
TTS_TEXT=Hello! This is a test of the text to speech system.
# TTS output base path without provider suffix (script adds -deepgram / -cartesia)
TTS_OUTPUT_BASE=files/tts-sample
#TTS provider override: deepgram | cartesia | both
TTS_PROVIDER=both
# TTS format: mp3 | wav - Cartesia
#TTS_FORMAT=wav
# TTS format: mp3 | opus | aac | flac | linear16 | mulaw | alaw - Deepgram
TTS_FORMAT=flac
