# Weather Agent KD - Example Environment Variables
# Copy this file to `.env` and fill in the values as needed. Do NOT commit your real secrets.

############################################
# Development Configuration (Optional)
############################################
NODE_ENV=development
PORT=3000
MASTRA_DEV_PORT=4000

############################################
# Mux API Configuration (Optional)
############################################
# Get credentials from https://dashboard.mux.com/settings
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
#MUX_WEBHOOK_SECRET=
#MUX_SIGNING_KEY=
#MUX_PRIVATE_KEY=
#MUX_AUTHORIZATION_TOKEN=
# MCP client args used by the Mux MCP clients (assets / uploads)
# Defaults are fine for most use cases; override only if you know you need to.
MUX_MCP_UPLOAD_ARGS=@mux/mcp,client=claude,--tools=dynamic,--resource=video.uploads
MUX_MCP_ASSETS_ARGS=@mux/mcp,client=claude,--tools=dynamic,--resource=video.assets
# Connection timeout bounds are enforced in code (min 5000, max 300000). Default 20000.
#MUX_CONNECTION_TIMEOUT=20000
# Optional interactive args used by clients; leave unset for defaults.
#MUX_MCP_INTERACTIVE_ARGS=
# Sample file path for test-mux-upload script
#MUX_SAMPLE_FILE=files/uploads/samples/mux-sample.wav

############################################
# Anthropic
############################################
# Get an API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=
# Optional: override the Anthropic model used for generation
# Defaults in code: "claude-3-haiku-20240307"
#ANTHROPIC_MODEL=claude-3-7-sonnet-20250219
#ANTHROPIC_MODEL=claude-3-haiku-20240307

############################################
# Speech: STT (Cartesia) and TTS (Cartesia, Deepgram)
############################################
# 1) Cartesia (STT + TTS)
# Get an API key: https://cartesia.ai/
CARTESIA_API_KEY=
# STT endpoints and model selection
CARTESIA_API_URL=https://api.cartesia.ai/stt
CARTESIA_VERSION=2025-04-16
CARTESIA_STT_MODEL=ink-whisper
CARTESIA_LANGUAGE=en
# TTS
CARTESIA_VOICE=1d3ba41a-96e6-44ad-aabb-9817c56caa68
# Optional: Override REST endpoint if needed
CARTESIA_TTS_HTTP_URL=/tts/bytes
# See https://docs.cartesia.ai/build-with-cartesia/tts-models
CARTESIA_TTS_MODEL=sonic-2
# Audio sample rate for Cartesia output (applies to mp3 and wav). Defaults to 44100 if unset.
#CARTESIA_SAMPLE_RATE=44100

# 2) Deepgram (TTS supported; STT optional depending on script)
# Get an API key: https://deepgram.com/
DEEPGRAM_API_KEY=
# Optional voice/model (default: "aura-asteria-en"). Code accepts DEEPGRAM_TTS_MODEL or DEEPGRAM_VOICE.
DEEPGRAM_VOICE=aura-asteria-en
#DEEPGRAM_TTS_MODEL=

############################################
# Weather.gov courtesy header (Optional, Recommended)
############################################
# Please set a descriptive user agent with contact info per Weather.gov guidelines.
# Example: "YourAppName/1.0 (youremail@example.com)"
WEATHER_MCP_USER_AGENT=YourAppName/1.0 (youremail@example.com)
WEATHER_DB_URL=file:./weather-agent-memory.db

############################################
# Misc (Optional)
############################################
# Mentioned in README for the minimal CLI variant; safe to leave unset here.
AGENT_NAME=weather-agent

############################################
# Test Scripts I/O (Optional)
############################################
# STT input audio file path (for src/mastra/scripts/test-stt.ts)
STT_INPUT_FILE=files/uploads/samples/mux-sample.wav
# STT output transcript file. If ends with .json, a JSON object with provider keys is saved.
# Otherwise a text file with labeled sections is saved.
STT_OUTPUT_FILE=files/sample-transcript.txt
# STT provider override: deepgram | cartesia | both
STT_PROVIDER=both

# TTS text input
TTS_TEXT=Hello! This is a test of the text to speech system.
# TTS output base path without provider suffix (script adds -deepgram / -cartesia)
TTS_OUTPUT_BASE=files/tts-
# TTS provider override: deepgram | cartesia | both
TTS_PROVIDER=both
# TTS formats: mp3 | wav - Cartesia
# TTS formats: mp3 | opus | aac | flac | linear16 | mulaw | alaw - Deepgram
TTS_FORMAT=mp3

